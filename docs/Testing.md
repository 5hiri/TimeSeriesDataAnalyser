# Testing Notes

## test.py

A minimal script that demonstrates applying a log transform to numeric columns in Financial-Analytics-data1.csv.

Example usage:

```bash
python test.py
```

This script is not a unit test suite and does not assert expected outcomes. It is primarily a quick manual check.
